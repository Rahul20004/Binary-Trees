class Solution {
    
    public static int getSize(Node node) {
        
        if (node == null) {
            return 0;
        }

        // Recursively left and right subtree and add 1 current node
        return 1 + getSize(node.left) + getSize(node.right);
    }
}
